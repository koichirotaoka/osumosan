<div class="top-wrapper">
    <div class="container">
      <h1>投稿詳細画面</h>
      <div>
        <% unless @house.user_id == current_user.id %>
          <% if @house.present? %>
            <%= link_to 'お気に入り解除する', house_path(id: @house.id), method: :delete, class: 'btn btn-danger' %>
          <% else %>
            <%= link_to 'お気に入りする', house_path(house_id: @house.id), method: :post, class: 'btn btn-primary' %>
          <% end %>
        <% end %>
        <%= link_to "ブログ一覧画面にもどる", houses_path, class: 'btn btn-primary' %>
      </div>
    </div>
  </div>
  
  <
 <script>
    $("#methods-test2").click(function(){
    $(this).button('dispose').removeAttr('data-toggle');
  });
  </script>

 